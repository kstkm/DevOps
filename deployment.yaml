apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
        - name: demo
          image: pacapu/devops:1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8181
          envFrom:
            - configMapRef:
                name: demo-config
            - secretRef:
                name: demo-secret